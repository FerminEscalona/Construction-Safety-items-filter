# Entorno objetivo: Python 3.11/3.12
# En Windows, TensorFlow corre en CPU de forma nativa.
# En macOS arm64 se mantiene el backend Metal usado originalmente por el notebook.

# Aprendizaje profundo
tensorflow==2.15.0; platform_system != "Darwin" or platform_machine != "arm64"
tensorflow-macos==2.15.0; platform_system == "Darwin" and platform_machine == "arm64"
tensorflow-metal==1.1.0; platform_system == "Darwin" and platform_machine == "arm64"
scipy
scikit-learn
ultralytics
pytest

# Datos y procesamiento
kagglehub
numpy
pandas
pillow
pyyaml
opencv-python

# Visualización y utilidades
matplotlib
tqdm

# Jupyter
jupyter
ipykernel
